import * as S from './header.style'

const Header = () => {
  return (
    <S.ContainerHeader>
      <S.TitleHeader>Dashboard</S.TitleHeader>
      <S.UserHeader>Usuário Logado</S.UserHeader>
    </S.ContainerHeader>
  )
}

export default Header
